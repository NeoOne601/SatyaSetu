/**
 * PROJECT SATYA: SECURE IDENTITY BRIDGE
 * =====================================
 * PHASE: 5.0 (The Signed Interaction)
 * VERSION: 1.2.2
 * DESCRIPTION: Fallback stub for analyzer type resolution.
 */

import 'identity_repo.dart';

IdentityRepository getIdentityRepository() => throw UnsupportedError(
  'Cannot create an IdentityRepository without native FFI support.',
);